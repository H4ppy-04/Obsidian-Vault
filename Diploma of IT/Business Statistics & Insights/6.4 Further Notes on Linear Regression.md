#### Important Slides:
 - slide number 6 (how to make predictions using linear regression)
 - Copy all 3 rules on slide  7 onto cheatsheet
 - Don't look at slide number 11 or the equation on slide 12 
 - Slide number 13 is important especially the information at the top pink box
	 - know what $R^2$ value is and know how to interpret it.
 - ==when going into the #wsta1250/final you won't be given a question like "what is $R^2$ but rather "how well does the model fit/predict the data?"== 
	 - The higher the R squared value the better the model fits the data.
 - 


- To determine if a linear regression model is suitable:
	- Consider the amount of data points in the linear model

### Making a Prediction Using the Fitted Line

If the **least squares regression line** is $\hat{y}=22.011 - 1.305x$, then the predicted price of a 10 year old car is $\bar{y}=22.011-1.305\times 10$.

### Rules for Valid Predictions
1. Only use the model to make predictions when a **statistically significant relation exists** (i.e. $H_0:\beta_1=0$ has been rejected)
2. Check that any prediction made is within the range of
the x-values used to obtain the regression line. We
cannot assume that the relation between X and Y is the
same outside the range used (this is called
extrapolation).
3. The regression model will only give valid predictions
when we predict Y (the outcome) from X (the
determinant). We cannot use the model to predict X
from Y.


### $R^2$ Coefficient of Determination
- We need to determine **how well the linear regression line fits the data**

![[Pasted image 20260907133105.png]]
- Residuals can be **positive** (above the line) or **negative** (below the line)
- The $R^2$ statistic is the measurement that determines how well the data fits the model (goodness-of-fit) based on the **residuals**.
- $R^2$ can be known as the **coefficient of determination**

The formula is:
$$R^2={\text{regression sum of squares}\over\text{total sum of squares}}=\frac{SSR}{SST}$$

